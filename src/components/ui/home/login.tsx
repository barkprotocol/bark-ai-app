import { AccountContent } from '@/app/(user)/account/account-content';

const LoginPage = () => {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      <AccountContent />
    </div>
  );
};

export default LoginPage;